# Examples

This directory holds example programs and memory images.

The current `.ini` files are legacy binary data from the original project. They
are preserved here until the assembler and compliance examples replace them with
documented OASIS programs.

Future examples should include:

- Source assembly in `.oas`
- Assembled instruction memory in `.mem`
- A short note describing expected register, memory, and debug-output results
